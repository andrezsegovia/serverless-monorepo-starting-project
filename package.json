{
    "name": "serverless-monorepo-starting-project",
    "version": "0.0.1",
    "scripts": {
        "bootstrap": "lerna clean && lerna link && lerna bootstrap"
    },
    "devDependencies": {
        "lerna": "^3.20.2"
    }
}
